# Copy to .env.local and fill in. Never commit the real .env.local.
NEXT_PUBLIC_SUPABASE_URL=https://agulweemteoeagscmppy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Must NEVER be prefixed NEXT_PUBLIC_ — that prefix is what
# tells Next.js a variable is safe to bundle into client JS, and this one
# is not. See lib/supabase-server.ts createServiceClient().
SUPABASE_SERVICE_ROLE_KEY=
